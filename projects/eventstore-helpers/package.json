{
  "name": "@eventstore-helpers/core",
  "version": "0.1.0",
  "description": "Helper functions for working with EventStoreDB streams and snapshots",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "@eventstore/db-client": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
